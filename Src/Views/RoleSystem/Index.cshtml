@model List<Microsoft.AspNetCore.Identity.IdentityRole>;
@{
    ViewData["Title"] = "Role Management Systeem";
    Layout = "~/Views/Shared/_Layout.cshtml";
}
<h1>Rol Magement Systeem</h1>
<form method="post" asp-action="" asp-controller="RoleManager">
    <div class="input-group">
        <input name="Role" class="form-control"></input>
        <span class="input-group-btn">
            <button class="btn btn-primary">Rol Toevoegen</button>
        </span>
    </div>
</form>
<table class="table table-striped">
    <thead>
        <tr>
            <th>Id</th>
            <th>Rol</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var role in Model)
        {
            <tr>
                <td>@role.Id</td>
                <td>@role.Name</td>
            </tr>
        }
    </tbody>
</table>